# 安全说明

## 这个项目的安全面（先说清楚它是什么）

本项目是一个**本地运行的研究/模拟工具**，不是网络服务。它的安全面很小：

- **没有服务端、没有认证、没有多用户**。仪表盘（Streamlit）默认监听本机，
  但 `streamlit run dashboard.py` 可以被 `--server.address 0.0.0.0` 暴露到局域网 ——
  **本仓库的脚本不会这么做**，如果你手动这么做了，那是你自己的暴露面；
- **只有出站 HTTP 请求**：访问新浪 / 腾讯 / 通达信的公开行情接口（见
  `ashare_quant/fetchers/`、`ashare_quant/realtime.py`）。没有入站接口；
- **不存储任何凭据**。仓库里没有 API key、token 或密码；
- **不执行远程代码**。模型文件由本仓库自己的 `joblib` 落盘与读取
  （`models/`，已 gitignore），**不加载来自第三方的 pickle**。

> ⚠️ `joblib.load` / `pickle` 本质上是不安全的格式。本项目只读取**自己训练产生**的
> `models/` 目录。**不要**把来路不明的 `.joblib` / `.pkl` 放进 `models/` 再让程序加载。

## 已知的"不是漏洞但会咬人"的行为

- **依赖第三方免费行情接口**：这些接口会限流（HTTP 501 WAF 反爬页）、
  会变更、会静默返回空。本项目已把"静默返回空"改成**显式抛错**，但接口本身不可控。
- **`scripts/sciverse_search.py`** 会从本机 `~/.codex/config.toml` 读取一个
  论文检索服务的 token 并放进子进程环境变量。它**不打印 token、不写盘**，
  但如果你不需要论文检索功能，可以直接删掉这个脚本。

## 报告问题

发现安全问题时，请**优先用 GitHub 的私密漏洞报告**
（仓库页 → Security → Report a vulnerability），不要直接开公开 issue。

如果该入口不可用，开一个 issue 但**只描述影响范围，不要贴可复现的攻击载荷**，
维护者会联系你换私密渠道。

## 支持范围

本项目是研究项目，**不提供任何 SLA**。维护者会尽力处理，
但请按"这是一个由 DSH（AI 编码 agent 运行时）维护的模拟研究项目"来设定预期。
