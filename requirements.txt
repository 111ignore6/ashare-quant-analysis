# 运行依赖（与 pyproject.toml 的 [project].dependencies 保持一致）
# 安装：pip install -r requirements.txt
pandas>=2.2
numpy>=1.26
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.4
lightgbm>=4.0
xgboost>=2.0
akshare>=1.12
baostock>=0.8.8
mootdx>=0.11
pyarrow>=15
PyYAML>=6.0
requests>=2.31
joblib>=1.3
plotly>=5.18
tqdm>=4.66

# 仪表盘（可选，但「一键启动」流程需要）
streamlit>=1.30
streamlit-autorefresh>=1.0

# 开发
pytest>=8.0
# ruff 钉小版本：0.16 改过默认规则集，不钉的话「全绿」会随上游发布漂移。
# 规则集在 pyproject.toml 的 [tool.ruff] 里显式声明（经典默认 E4/E7/E9/F）。
ruff>=0.16,<0.17
