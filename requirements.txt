# Runtime dependencies (kept in sync with pyproject.toml [project].dependencies)
#
# WARNING: KEEP THIS FILE PURE ASCII.
# pip decodes a requirements file using the *system locale* encoding, NOT UTF-8
# (pip's auto_decode only honours a BOM, otherwise it uses
# locale.getpreferredencoding()). On a Chinese Windows box that is cp936/GBK,
# so any non-ASCII comment here makes the very first documented command fail:
#     pip install -r requirements.txt
#     -> UnicodeDecodeError: 'gbk' codec can't decode byte 0xa1 in position 6
# Chinese docs live in README.md / AGENTS.md instead. (2026-09-23)
pandas>=2.2
numpy>=1.26
scipy>=1.11
statsmodels>=0.14
scikit-learn>=1.4
lightgbm>=4.0
xgboost>=2.0
akshare>=1.12
baostock>=0.8.8
mootdx>=0.11
pyarrow>=15
PyYAML>=6.0
requests>=2.31
joblib>=1.3
plotly>=5.18
tqdm>=4.66

# Dashboard. Note: `pip install -e .` does NOT install these --
# they live in the [project.optional-dependencies] "dashboard" extra.
# Use `pip install -e ".[dashboard]"` if you install the package that way.
streamlit>=1.30
streamlit-autorefresh>=1.0

# Dev
pytest>=8.0
# Pin ruff to a minor range: 0.16 changed its default rule set, so without a pin
# "all green" silently drifts with upstream releases.
# The rule set is declared explicitly in pyproject.toml [tool.ruff].
ruff>=0.16,<0.17
