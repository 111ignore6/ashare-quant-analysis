# 贡献指南

> 本项目**由 AI 维护**，人类机主负责方向与验收。欢迎 issue 与 PR。

## 最重要的一条：本项目接受"推翻既有结论"的贡献

这个仓库的价值观是**结论必须能被证伪**，而不是"看起来对"。所以：

- 如果你发现 `AGENTS.md` / `docs/HONESTY.md` / README 里的某个结论是**错的**，
  **欢迎直接指出来** —— 那正是本项目最需要的贡献；
- 请**附上可复现的命令与原始输出**。本项目不接受"我认为应该是这样"；
- 被推翻的旧结论**不会被删除**，而是标注作废并保留原文（见 `AGENTS.md` 的写法）。
  这是刻意的：知道"当初为什么那么想"是防止重犯的材料。

## 提交前必须全绿

```bash
python -m pytest tests/          # 期望：221 passed（数字会随功能增加而变）
python -m ruff check .           # 期望：All checks passed!
```

项数以 `python -m pytest tests/ --collect-only -q` 的**实测**为准，不要照抄本文档的数字。

> ⚠️ **ruff 必须装对版本**：规则集在 `pyproject.toml` 的 `[tool.ruff]` 里**显式声明**，
> ruff 本体钉在 `>=0.16,<0.17`。**不要把 `select` 删掉改成依赖默认值** ——
> 2026-09-23 真出过事：仓库当时没有任何 ruff 配置，而 ruff 0.16 扩大了默认规则集，
> 于是同一个 commit **本地（0.15）全绿、CI（0.16）红 179 项**。
> 一条标准如果没被写下来，它的含义就会随上游发布静默漂移。
> 升级 ruff 时请同时改 `pyproject.toml` 与 `.github/workflows/ci.yml`，并重跑全量检查。

## 判据设计纪律（**改代码前必读**）

这几条是被真实事故逼出来的，违反它们会造出"不会熔断的保险丝"：

1. **新增的判据/断言，必须先在"还原后的旧行为"上跑出红。**
   做法：把修复行还原到一份 scratch 副本，再跑新测试，确认它真的**红**。
   只看到新代码下是绿的，不能证明这条测试有效。
   —— 反面案例：本项目的面板缓存回归测试第一次写错了（忘了建指数 manifest），
   导致缓存**永远**失效，于是在旧代码上也全绿。
2. **"注入旧行为"这一步本身也要自证。** 注入后先断言目标特征串**确实消失**，再跑测试。
   前置断言不成立就中止。真栽过：注入时把新逻辑改名保留，特征串仍在文件里 → 测试当然还绿。
3. **判据不得自指。** 不能用同一份数据既产出结论、又产出"验证该结论的基准"——
   故障态下必然自洽。真栽过：一份验收脚本用自己的产物当判据，**在最需要它的那天报了全绿**。
4. **自述指标必须与实测对撞。** 例如验收里应有硬断言
   `stats["stocks_behind"] == 用 parquet 元数据统计法算出的实测落后只数`。
5. **阴性结果必须自带覆盖度**（读了多少文件、日志保留多久），否则是伪阴性；
   **阳性结果也要自证合理性**（"163/163 全中"通常是探针坏了，不是世界这么整齐）。
6. **未证实的因果不配获得行动建议。** 假设一旦降级为假设，
   据它给出的"你该怎么做"必须同时撤回。

## 改这几类代码时的额外要求

| 改动 | 必须做的事 |
|---|---|
| **解析成交量** | 必须调用 `venues.volume_in_shares`，**并补一条 688（科创板）的测试**。这个坑已经犯过 3 次 |
| **改复权逻辑** | 抽查全市场历史 NaN 率，别只看最新几行 |
| **换数据源** | 先跑 `python scripts/fix_volume_unit.py`（dry-run，应报"需要修复 0 只"），再跑 `python scripts/audit_qfq_anchor.py` |
| **批量改写历史数据** | dry-run 默认 / 备份 / **方向取自独立源** / 幂等自检 / 全量对撞验收 / 重建三件套并核对指纹 |
| **改 `build_dataset` 的特征列/窗口/语义** | **必须手动 `FEATURE_CACHE_VERSION += 1`**，否则旧缓存会被静默复用 |
| **改仪表盘** | 至少跑 `tests/test_dashboard_smoke.py`；注意**禁止在 `st.cache_data` 函数里嵌套调用另一个 `st.cache_data` 函数** |
| **改 `.ps1` 脚本** | Windows PowerShell 5.1 读中文 .ps1 **需要 UTF-8 BOM**；改完要重新注册计划任务才生效 |

## 关于 `AGENTS.md`

`AGENTS.md` 是 AI 维护者的项目级长期记忆（76 KB）。**如果你用编码 agent 操作本仓库，
请先让它读这个文件** —— 里面记录了绝大多数踩过的坑，能省掉大量重复排查。

改动 `AGENTS.md` 时请保持它的既有写法：

- 结论要标注**证据强度**（实测 / 引用 / 推断 / 未证实）；
- 被推翻的旧结论**保留原文并标注作废**，不要直接删掉；
- 未证实的东西明确写"未证实"，**不要给它配行动建议**。

## 提交信息

用 `<type>(<scope>): <subject>` 形式，例如：

```
fix(fetchers): 科创板成交量不再无条件 ×100
docs: 记录 2026-09-18 事故的修复、证据与边界
```

## 免责声明

本项目是**模拟研究**，不构成投资建议。请勿把它当作实盘交易系统使用。
